package com.example.barfinder.ui.placelist.scene

import android.view.View
import androidx.recyclerview.widget.RecyclerView

/**
 * The viewHolder to a single place in the list.
 *
 * @param v the view that inflates this viewholder
 */
class PlaceViewHolder (v: View) : RecyclerView.ViewHolder(v) {

    init {

    }
}